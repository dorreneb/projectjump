<%- model_class = Project -%>
<div class="span12">
  <h1><%=t '.title', :default => @project.name %></h1>


  <!--<dl class="dl-horizontal">
    <dt><strong><%= model_class.human_attribute_name(:active) %>:</strong></dt>
    <dd><%= @project.active %></dd>
    <dt><strong><%= model_class.human_attribute_name(:description) %>:</strong></dt>
    <dd><%= @project.description %></dd>
  </dl>-->

  <ul class="nav nav-tabs">
    <li class="active" id="tab-overview">
      <a href="#" onclick="swap('overview')">Overview</a>
    </li>
    <li id="tab-contactinfo">
      <a href="#" onclick="swap('contactinfo')">Contact Information</a>
    </li>
    <li id="tab-blog">
      <a href="#" onclick="swap('blog')">Blog</a>
    </li>
  </ul>

  <div class="information" id="overview">
    <%= @project.description %>
  </div>
  <div class="information" id="contactinfo">
    <dl class="dl-horizontal">
      <dt><%= model_class.human_attribute_name(:task_tracker_url) %></dt>
      <dd><%= @project.task_tracker_url %></dd>
      <dt><%= model_class.human_attribute_name(:repo_url) %></dt>
      <dd><%= @project.repo_url %></dd>
      <dt><%= model_class.human_attribute_name(:owner_id) %></dt>
      <dd><%= User.find(@project.owner_id).email %></dd>
    </dl>
  </div>
  <div class="information" id="blog">
    Blog will go here
  </div>

  <!--
  <div class="form-actions">
    <%= link_to t('.back', :default => t("helpers.links.back")),
                projects_path, :class => 'btn'  %>
    <%= link_to t('.edit', :default => t("helpers.links.edit")),
                edit_project_path(@project), :class => 'btn' %>
    <%= link_to t('.destroy', :default => t("helpers.links.destroy")),
                project_path(@project),
                :method => 'delete',
                :data => { :confirm => t('.confirm', :default => t("helpers.links.confirm", :default => 'Are you sure?')) },
                :class => 'btn btn-danger' %>
  </div>
-->
</div>